export interface ItemProps {
  id: string;
  item: string;
  quantity: number;
  description: string;
}

export interface AddItemArgumentProps {
  item: string;
  quantity: number;
  description: string;
}
